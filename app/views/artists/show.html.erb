<h1><%= @artist.name %></h1>
<h2><%= link_to 'Songs', artist_songs_path(@artist) %></h2>
<ul>
  <% @artist.songs.each do |song| %>
    <li><%= link_to song.title, artist_song_path(song) %></li>
  <% end %>
</ul>
